# Odoo MCP Server Dependencies

# Core dependencies
requests>=2.28.0
python-dotenv>=1.0.0

# MCP library (optional, for MCP server mode)
mcp>=1.0.0

# HTTP server (optional, for HTTP transport mode)
starlette>=0.27.0
uvicorn>=0.20.0
