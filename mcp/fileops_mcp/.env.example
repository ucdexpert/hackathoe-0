# FileOps MCP Server - Environment Variables

# =============================================================================
# Browser Configuration
# =============================================================================
# Browser type: chromium, firefox, or webkit
BROWSER_TYPE=chromium

# Run browser in headless mode (no GUI)
BROWSER_HEADLESS=true

# Browser timeout in seconds
BROWSER_TIMEOUT=30

# =============================================================================
# Session Configuration
# =============================================================================
# Directory for persistent browser sessions (cookies, login state)
# LinkedIn login will be saved here for reuse
SESSION_DIR=D:/hackathons-Q-4/hackthon-0/AI_Employee_Vault/.browser_session

# =============================================================================
# Vault Configuration
# =============================================================================
# Path to AI_Employee_Vault root directory
VAULT_PATH=D:/hackathons-Q-4/hackthon-0/AI_Employee_Vault

# =============================================================================
# File Operations Security
# =============================================================================
# Comma-separated list of allowed directories for file operations
# Files outside these directories will be rejected
ALLOWED_DIRECTORIES=D:/hackathons-Q-4/hackthon-0/AI_Employee_Vault

# =============================================================================
# Usage Instructions:
# =============================================================================
# 1. Copy this file to .env
# 2. Adjust paths for your system
# 3. NEVER commit .env file to git (it's in .gitignore)
#
# To test:
#   python server.py
#   {"method": "file.list_files", "params": {"directory": ".", "pattern": "*.md"}}
#
